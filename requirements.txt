# Python音乐播放器依赖

# GUI框架
PyQt6>=6.4.0

# 音频播放
pygame>=2.5.0

# 高级音频后端 (可选)
miniaudio>=1.59       # 推荐：Gapless/Crossfade/EQ 支持
python-vlc>=3.0.18122 # 备选：ReplayGain 原生支持，需安装 VLC 播放器

# 元数据解析
mutagen>=1.46.0

# 配置管理
PyYAML>=6.0

# 图像处理
Pillow>=10.0.0

# 网络搜索
ddgs>=6.1.0  # DuckDuckGo 搜索，用于辅助 AI 打标签

# 开发依赖
pytest>=7.0.0
pytest-qt>=4.2.0
